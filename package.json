{
  "name": "persoenlicher-assistent",
  "version": "1.0.0",
  "private": true,
  "description": "Persönlicher Assistent: KI-Chat, Aufgaben, Notizen und Klausurvorbereitung",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix web && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
